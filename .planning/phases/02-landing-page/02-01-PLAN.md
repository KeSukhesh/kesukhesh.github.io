<plan>
  <phase>2</phase>
  <plan-number>01</plan-number>
  <type>execute</type>
  <name>Build 5-Section Scroll-Snap Landing Page</name>
</plan>

<objective>
Build the full landing page with 5 scroll-snap sections (Hero, About, Projects, Blog Teaser, Contact), scroll progress bar, IntersectionObserver reveal animations, and hero letter-drop effect.

Traces to: PRD §3 US-001 (Hero), US-003 (About), US-004 (Projects), US-005 (Blog Teaser), US-006 (Contact/Footer)
</objective>

<context>
- Discovery: .planning/phases/02-landing-page/DISCOVERY.md
- PRD: .planning/specs/PRD.md
- Existing: src/pages/index.astro (placeholder), src/layouts/BaseLayout.astro, src/components/Header.astro, Footer.astro, ThemeToggle.astro
- Design tokens: src/styles/global.css (ink, paper, forest, muted, border + dark variants)
- Typography: Libre Baskerville (serif), Space Mono (mono)
</context>

<tasks>
  <task type="auto">
    <name>Add scroll-snap CSS and reveal animation infrastructure</name>
    <files>src/styles/global.css</files>
    <action>
      Add to global.css:
      1. Scroll-snap rules scoped to landing page via `.snap-page` class on html:
         - `scroll-snap-type: y mandatory` on html.snap-page
         - Sections get `scroll-snap-align: start`
      2. Reveal animation classes:
         - `.reveal`: opacity-0, translate-y-8, transition opacity+transform 0.6s ease-out
         - `.revealed`: opacity-1, translate-y-0
      3. Scroll progress bar styles:
         - Fixed bar at top, height 3px, bg-forest, z-60, width via CSS custom property
      4. Hero letter-drop keyframes:
         - `@keyframes letter-drop` — translateY(-100%) to translateY(0) with opacity
         - `.letter-drop` utility class with animation-fill-mode: both
      5. Bouncing scroll indicator keyframe
    </action>
    <verify>
      - [ ] `.snap-page` class enables scroll snapping on html
      - [ ] `.reveal` / `.revealed` classes exist with transitions
      - [ ] `@keyframes letter-drop` defined
      - [ ] Scroll progress bar styles defined
    </verify>
    <done>Animation and scroll-snap CSS infrastructure ready</done>
  </task>

  <task type="auto">
    <name>Create ScrollReveal component</name>
    <files>src/components/ScrollReveal.astro</files>
    <action>
      Create a reusable IntersectionObserver script component:
      - On DOMContentLoaded, observe all `.reveal` elements
      - On intersection (threshold: 0.1), add `.revealed` class
      - Once revealed, unobserve the element
      - Re-initialize on `astro:after-swap` for view transitions
    </action>
    <verify>
      - [ ] Elements with `.reveal` class animate in on scroll
      - [ ] Animation triggers once and stops observing
    </verify>
    <done>ScrollReveal component with IntersectionObserver working</done>
  </task>

  <task type="auto">
    <name>Create ScrollProgress component</name>
    <files>src/components/ScrollProgress.astro</files>
    <action>
      Create scroll progress bar component:
      - Fixed div at top of page, 3px height, forest green, z-60
      - Vanilla JS: listen to scroll event, calculate percentage, set width via transform scaleX
      - Use requestAnimationFrame for performance
      - Re-initialize on `astro:after-swap`
    </action>
    <verify>
      - [ ] Progress bar visible at top of page
      - [ ] Width updates smoothly on scroll
      - [ ] Uses forest green color
    </verify>
    <done>Scroll progress bar tracks page position</done>
  </task>

  <task type="auto">
    <name>Build Hero section</name>
    <files>src/pages/index.astro</files>
    <action>
      Replace placeholder content with Hero section:
      - Full viewport height, centered content, snap-start
      - "Sukhesh Patro" with letter-drop animation — each letter wrapped in span with staggered animation-delay
      - Tagline "Software engineer, builder, thinker." fades in after name animation completes
      - Social links row (GitHub, LinkedIn, Email) — reuse SVG icons from Footer
      - Bouncing scroll-down chevron indicator at bottom
      - Section id="hero"
    </action>
    <verify>
      - [ ] Hero fills viewport
      - [ ] Letters animate in with stagger
      - [ ] Tagline appears after name
      - [ ] Social links present and clickable
      - [ ] Scroll indicator bounces
    </verify>
    <done>Hero section with letter-drop animation</done>
  </task>

  <task type="auto">
    <name>Build About section</name>
    <files>src/pages/index.astro</files>
    <action>
      Add About section after Hero:
      - Section id="about", full viewport, snap-start
      - Heading "About" with reveal animation
      - Bio paragraph about Sukhesh — software engineer at Lyra Technologies, UNSW graduate
      - Highlight chips/badges: Lyra Technologies, UNSW, Automation, AI/ML, Full-Stack
      - All content elements use `.reveal` class for scroll-in animation
    </action>
    <verify>
      - [ ] About section fills viewport
      - [ ] Content reveals on scroll
      - [ ] Bio text and highlights readable
    </verify>
    <done>About section with bio and reveal animations</done>
  </task>

  <task type="auto">
    <name>Build Projects section</name>
    <files>src/pages/index.astro</files>
    <action>
      Add Projects section after About:
      - Section id="projects", full viewport, snap-start
      - Heading "Projects" with reveal animation
      - Two project cards in a grid:
        - Sheaf: placeholder description, link out (GitHub)
        - FishSOOP: placeholder description, link out (GitHub)
      - Cards have: title, description, tech tags, hover effect (subtle lift + border-forest)
      - Cards use `.reveal` with staggered delays
    </action>
    <verify>
      - [ ] Projects section fills viewport
      - [ ] Two cards visible with hover effects
      - [ ] Cards reveal with stagger on scroll
    </verify>
    <done>Projects section with Sheaf and FishSOOP cards</done>
  </task>

  <task type="auto">
    <name>Build Blog Teaser section</name>
    <files>src/pages/index.astro</files>
    <action>
      Add Blog Teaser section after Projects:
      - Section id="blog", full viewport, snap-start
      - Heading "Writing" or "Blog" with reveal animation
      - 2-3 hardcoded blog post entries (title, date, short excerpt)
      - Each entry is a link-styled row with hover effect
      - "View all posts →" link to /blog
      - All elements use `.reveal` class
      Note: Hardcoded for now — Phase 3 will make this dynamic via content collections
    </action>
    <verify>
      - [ ] Blog teaser section fills viewport
      - [ ] Placeholder posts visible
      - [ ] "View all posts" link present
    </verify>
    <done>Blog teaser section with placeholder posts</done>
  </task>

  <task type="auto">
    <name>Build Contact section and wire up footer</name>
    <files>src/pages/index.astro</files>
    <action>
      Add Contact section before Footer:
      - Section id="contact", full viewport, snap-start
      - Heading "Get in Touch" with reveal animation
      - Short message encouraging contact
      - Email link (mailto:sukhesh@kesukhesh.com) styled as a prominent button/link
      - Social links row (GitHub, LinkedIn)
      - Footer renders within or immediately after this section
    </action>
    <verify>
      - [ ] Contact section fills viewport
      - [ ] Email link works
      - [ ] Social links clickable
    </verify>
    <done>Contact section with email and social links</done>
  </task>

  <task type="auto">
    <name>Wire up landing page layout with scroll-snap</name>
    <files>src/pages/index.astro, src/layouts/BaseLayout.astro</files>
    <action>
      1. In index.astro: Import and include ScrollReveal + ScrollProgress components
      2. Add a script that adds `snap-page` class to html on the landing page only
      3. Ensure BaseLayout main wrapper doesn't interfere with snap (remove pt-16 on landing or adjust)
      4. Hide fixed header initially on hero, show on scroll (optional — or keep visible)
      5. Ensure scroll-snap works correctly with all 5 sections
      6. Test dark mode with all sections
    </action>
    <verify>
      - [ ] Scroll snaps between all 5 sections
      - [ ] Progress bar updates correctly
      - [ ] Reveal animations fire on each section
      - [ ] Dark mode works on all sections
      - [ ] `npm run build` succeeds
    </verify>
    <done>Full landing page assembled with scroll-snap and animations</done>
  </task>
</tasks>

<verification>
  - [ ] `npm run build` succeeds with zero errors
  - [ ] All 5 sections render and fill viewport
  - [ ] Scroll-snap moves between sections cleanly
  - [ ] Hero letter-drop animation plays on load
  - [ ] IntersectionObserver reveals fire on each section
  - [ ] Scroll progress bar tracks position
  - [ ] Dark mode works across all sections
  - [ ] Mobile responsive — sections stack and readable
  - [ ] Social links and email link functional
  - [ ] Nav anchor links (#about, #projects) scroll to correct sections
</verification>

<success_criteria>
A polished 5-section scroll-snap landing page with animated hero, reveal animations, scroll progress bar, and full dark mode support. All sections responsive and accessible. Ready for Phase 3 (Blog Engine) to replace hardcoded blog teaser with dynamic content.
</success_criteria>
