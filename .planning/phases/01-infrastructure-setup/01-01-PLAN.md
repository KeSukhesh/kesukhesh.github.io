<plan>
  <phase>1</phase>
  <plan-number>01</plan-number>
  <type>execute</type>
  <name>Scaffold Astro Project with Design System</name>
</plan>

<objective>
Set up the full Astro project with Tailwind CSS, design tokens (colors, typography), base layout with navigation and footer, dark/light theme toggle with persistence, and GitHub repo initialization. This is the foundation everything else builds on.

Traces to: PRD §4 (Stack), US-011 (Theme toggle), US-012 (Typography & color)
</objective>

<context>
- PRD: .planning/specs/PRD.md
- Project: .planning/PROJECT.md
- Stack: .planning/codebase/STACK.md
- Structure: .planning/codebase/STRUCTURE.md
- Reference design: /tmp/claude-maxxing-ref/index.html (scroll-snap, IntersectionObserver patterns)
</context>

<tasks>
  <task type="auto">
    <name>Scaffold Astro project</name>
    <files>package.json, astro.config.mjs, tsconfig.json</files>
    <action>
      Run `npm create astro@latest` in /Users/sukie/projects/kesukhesh with:
      - Template: empty/minimal
      - TypeScript: yes (strict)
      - Install deps: yes

      Then install additional deps:
      - `npm install @astrojs/tailwind tailwindcss fuse.js`
      - `npm install -D @astrojs/sitemap`

      Configure astro.config.mjs:
      - Add tailwind integration
      - Add sitemap integration
      - Set site to 'https://kesukhesh.github.io'
      - Set output to 'static'
    </action>
    <verify>
      - [ ] `npm run dev` starts without errors
      - [ ] package.json has astro, tailwind, fuse.js deps
      - [ ] astro.config.mjs has tailwind + sitemap integrations
    </verify>
    <done>Astro dev server runs on localhost</done>
  </task>

  <task type="auto">
    <name>Configure Tailwind with design tokens</name>
    <files>src/styles/global.css, tailwind.config.mjs</files>
    <action>
      Create global.css with Tailwind directives.

      Configure tailwind.config.mjs with:
      - Colors: ink (#0a0a0a), paper (#f5f5f0), forest (#2d5a3d), forest-light (#3a7a52), forest-dark (#1e3d29), muted (#6b6b6b)
      - Fonts: serif (Libre Baskerville), mono (Space Mono)
      - Content paths for .astro files

      Add Google Fonts link in base layout (Libre Baskerville 400/700, Space Mono 400/700)
    </action>
    <verify>
      - [ ] Tailwind classes resolve correctly
      - [ ] Custom colors available (bg-forest, text-ink, etc.)
      - [ ] Fonts load from Google Fonts
    </verify>
    <done>Design tokens accessible via Tailwind utilities</done>
  </task>

  <task type="auto">
    <name>Build BaseLayout with nav and footer</name>
    <files>src/layouts/BaseLayout.astro, src/components/Header.astro, src/components/Footer.astro</files>
    <action>
      BaseLayout.astro:
      - HTML shell with lang="en", proper meta tags
      - Google Fonts preconnect + link
      - Inline script in <head> for theme (read localStorage, set class, prevent FOUC)
      - Slot for page content
      - Import Header + Footer

      Header.astro:
      - Fixed/sticky nav
      - Logo/name "Sukhesh Patro" linking to /
      - Nav links: Blog (/blog), Projects (#projects), About (#about)
      - Theme toggle button
      - Mobile hamburger menu

      Footer.astro:
      - Social links: GitHub, LinkedIn, Email
      - Copyright line
      - Minimal design
    </action>
    <verify>
      - [ ] BaseLayout renders with nav and footer
      - [ ] Nav links work (Blog routes to /blog, anchors scroll on landing)
      - [ ] Mobile menu toggles open/close
    </verify>
    <done>Base layout with responsive nav and footer</done>
  </task>

  <task type="auto">
    <name>Implement dark/light theme toggle</name>
    <files>src/components/ThemeToggle.astro</files>
    <action>
      ThemeToggle.astro:
      - Sun/moon icon toggle button
      - Client-side script:
        - Read theme from localStorage, fallback to prefers-color-scheme
        - Toggle 'dark' class on <html>
        - Persist choice to localStorage
      - Inline script in BaseLayout <head> to prevent FOUC:
        - Read localStorage before render
        - Set class immediately

      Tailwind dark mode: class strategy (dark: prefix)

      CSS variables approach:
      - Dark: bg-ink, text-paper
      - Light: bg-paper, text-ink
      - Transitions on theme change (0.3s)
    </action>
    <verify>
      - [ ] Toggle switches between dark and light
      - [ ] Preference persists across page reloads
      - [ ] No flash of wrong theme on initial load
      - [ ] Respects system preference on first visit
    </verify>
    <done>Theme toggle works with persistence and no FOUC</done>
  </task>

  <task type="auto">
    <name>Initialize git repo and create placeholder index</name>
    <files>.gitignore, src/pages/index.astro, src/pages/404.astro</files>
    <action>
      Create a placeholder index.astro that uses BaseLayout and shows:
      - "Sukhesh Patro" heading
      - "Coming soon" or similar
      - This verifies the full stack works end-to-end

      Create 404.astro with BaseLayout wrapper.

      Initialize git:
      - git init
      - Create .gitignore (node_modules, dist, .astro)
      - Initial commit
      - Create GitHub repo via `gh repo create kesukhesh/kesukhesh.github.io --public`
      - Push
    </action>
    <verify>
      - [ ] `npm run build` succeeds
      - [ ] `npm run dev` shows placeholder page with correct fonts and colors
      - [ ] Git repo initialized with clean first commit
      - [ ] GitHub repo created and code pushed
    </verify>
    <done>Working Astro project on GitHub with placeholder page</done>
  </task>
</tasks>

<verification>
  - [ ] `npm run dev` starts and serves pages
  - [ ] `npm run build` succeeds with zero errors
  - [ ] Dark/light toggle works with no FOUC
  - [ ] Libre Baskerville + Space Mono fonts load
  - [ ] Forest green accent color visible
  - [ ] Responsive nav with mobile hamburger
  - [ ] Git repo on GitHub with clean initial commit
</verification>

<success_criteria>
A fully scaffolded Astro project with Tailwind, design system (typography + colors), responsive nav/footer, working dark/light toggle, and code pushed to GitHub. Ready for landing page sections (Phase 2) and blog engine (Phase 3).
</success_criteria>
