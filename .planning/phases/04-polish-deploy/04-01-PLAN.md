# Plan 04-01: SEO, Deploy & Final Polish

## Goal
Add Open Graph meta tags, canonical URLs, robots.txt, GitHub Actions deploy workflow, and verify the final build.

## Tasks

### Task 1: SEO Meta Tags in BaseLayout
<task>
Update `src/layouts/BaseLayout.astro`:
- Add `og:title`, `og:description`, `og:type`, `og:url` meta tags
- Add `og:site_name` as "Sukhesh Patro"
- Add Twitter card meta tags (twitter:card, twitter:title, twitter:description)
- Add canonical URL via `<link rel="canonical">`
- Use Astro.url for canonical/og:url
- Accept optional `ogType` prop (default "website", "article" for blog posts)
</task>
<verify>
- Build succeeds
- Meta tags present in generated HTML
</verify>

### Task 2: BlogLayout OG Enhancement
<task>
Update `src/layouts/BlogLayout.astro`:
- Pass `ogType="article"` to BaseLayout
- Add article-specific meta: `article:published_time`, `article:tag`
</task>
<verify>
- Blog post pages have article OG tags
</verify>

### Task 3: robots.txt
<task>
Create `public/robots.txt`:
- Allow all user agents
- Point to sitemap at site URL
</task>
<verify>
- File copied to dist/robots.txt on build
</verify>

### Task 4: GitHub Actions Deploy Workflow
<task>
Create `.github/workflows/deploy.yml`:
- Trigger on push to main branch
- Use Node 22
- Install deps, build with Astro
- Deploy to GitHub Pages using official actions
- Use `withastro/action@v3` for build step
- Use `actions/deploy-pages@v4` for deployment
</task>
<verify>
- YAML is valid
- Workflow references correct Node version and branch
</verify>

### Task 5: Final Build Verification
<task>
Run `npm run build` and verify:
- All pages generate correctly
- Check JS bundle size (should be minimal)
- Verify sitemap, robots.txt in dist
- Check OG tags in generated HTML
</task>
<verify>
- Build completes without errors
- All expected pages present
- robots.txt and sitemap in dist
</verify>

## Verification
<verification>
1. `npm run build` completes without errors
2. OG meta tags in generated HTML (landing page + blog posts)
3. Canonical URLs correct
4. robots.txt in dist with sitemap reference
5. GitHub Actions workflow file is valid YAML
6. Minimal JS bundle (only search + theme + animations)
</verification>

## Files Created/Modified
- `src/layouts/BaseLayout.astro` (MODIFIED — OG tags, canonical)
- `src/layouts/BlogLayout.astro` (MODIFIED — article meta)
- `public/robots.txt` (NEW)
- `.github/workflows/deploy.yml` (NEW)
